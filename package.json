{
  "name": "origin-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev --filter ./packages",
    "release": "pnpm -r release --filter ./packages"
  },
  "prettier": "@app-research/origin-vue/config/prettier",
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@app-research/eslint-config": "^0.1.5",
    "@app-research/origin-vue": "workspace:*",
    "@types/node": "^16.11.6",
    "eslint": "^8.1.0",
    "prettier": "^2.4.1",
    "standard-version": "^9.3.2",
    "typescript": "^4.4.4"
  }
}